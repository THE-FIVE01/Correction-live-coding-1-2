
// Création de la classe photos

class Photos {
  int albumId;
  int id;
  String title;
  String url;
  String thumbnailUrl;

  // Constructeur ou init
  Photos({this.albumId, this.id, this.title, this.url, this.thumbnailUrl});
  
}

