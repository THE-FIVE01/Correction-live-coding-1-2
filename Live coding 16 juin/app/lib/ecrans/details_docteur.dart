

import 'package:flutter/material.dart';

class DetailsDocteur extends StatelessWidget {
  static const routeName = '/details-docteur';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}